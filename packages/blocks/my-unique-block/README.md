# blocks-my-unique-block

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build blocks-my-unique-block` to build the library.

## Running unit tests

Run `nx test blocks-my-unique-block` to execute the unit tests via [Jest](https://jestjs.io).
